# Normalize line endings across Windows/macOS/Linux — text is LF in the repo.
* text=auto eol=lf

# Explicit binary (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
*.mp4 binary
*.ico binary
