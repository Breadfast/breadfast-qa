# Certification Findings Register

> Deviations discovered during the Platform Certification campaign. Per the phase rules, these are **certification findings** (to be fixed as part of achieving pilot readiness), **not feature requests**. Severity: Critical / High / Medium / Low / Info. Status: Open / Fixed / Deferred / Won't-fix.

| ID | Sev | Area (node) | Finding | Evidence | Recommendation | Status |
|----|-----|-------------|---------|----------|----------------|:------:|
| **F1** | High | execution / html_report | Execution **blocked by an environment 502 outage** was not surfaced as a **BLOCKER**. The report reads "execution pending / NOT RUN" with 0 defects and no environment-limitation flag — indistinguishable from "not attempted." | B10-56757 `test_report` (coverage "not run"; 0/0/0); `exploratory_10/11` (401/502, perks page stuck). | Detect repeated auth/5xx during execution → set run/combo state to **BLOCKED** and emit an **environment-limitation** note (per bug-reporting.md) in the report + Review Confidence, rather than an empty pass-through. | Open |
| **F2** | High | gate_file_bugs / exploratory | **Defect fidelity gap:** a real product-defect candidate found during exploratory probing was **not filed**. `defects.md` = "No defects found" despite AC-11 evidence. | `exploratory_09_save_order_false_positive.png` (Save order visible with no order change); `exploratory_06_sort_click_no_reorder_bug.png`. | Route exploratory `findings[]` into the defect candidate set (or a "candidate defects" report section) so hands-on findings aren't lost when formal execution is blocked. | Open |
| **F3** | Medium | html_report | The report renders exploratory **`riskAreas`** (charters) but **not** the captured exploratory **`findings`** (observation + screenshot). Real hands-on findings are invisible in the artifact. | `renderReport` `exploratoryBlock` = `list(riskAreas)` only; `ExploratoryNotes.findings[]` unused in the report. | Add an "Exploratory findings" subsection rendering `findings[]` (observation + screenshot link). Deterministic, small, 0 AI. | Open |
| **F4** | Info | (whole run) | The certified B10-56757 run **predates Phase 2** — the report lacks Story Health / Recommendations / Parity / Review Confidence / Visual Intelligence / Activity Timeline / Knowledge Lint sections. | Report dated 2026-07-14; structure matches pre-Phase-2 `renderReport`. | Re-run on a healthy environment to certify the Phase-2 intelligence sections end-to-end. Not a defect. | Open |
| **F5** | Low | gate_push_hls / gate_upload_browserstack | **External-write verification not captured:** no artifact confirms the HLS was pushed to the Jira checklist; BrowserStack import evidence exists (`bs_import_result.png`) but folder/count-match isn't recorded. | Local `hls/hls.md` present; no Jira-push confirmation; `bs_import_result.png` present. | Capture explicit confirmations (Jira checklist URL/screenshot; BrowserStack folder count) as run evidence for auditability. | Open |

## Notes
- **No blocking platform *defect* (crash/data-loss) was found.** F1–F3 are handling/fidelity gaps around the blocked-execution path; F4 is a coverage note; F5 is evidence hygiene.
- All fixes implied here are **deterministic** (report rendering, blocker detection, exploratory→defect routing) and consistent with ADR-001 (0 new AI).
- These findings feed §10–§11 of the [certification-report.md](./certification-report.md) and the go/no-go assessment.
